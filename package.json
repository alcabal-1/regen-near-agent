{
  "name": "regen-near-agent",
  "version": "0.1.0",
  "description": "An autonomous agent that scores a regenerative project, emits a proof-plan, and acts on-chain by issuing a credit tied to the proof-plan hash. Proof-plans, not certification.",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "demo": "tsx --env-file-if-exists=.env src/index.ts",
    "demo:near": "tsx --env-file-if-exists=.env src/index.ts --near",
    "demo:research": "tsx --env-file-if-exists=.env src/index.ts --research --near",
    "demo:glm": "tsx --env-file-if-exists=.env src/index.ts --glm",
    "demo:tigris": "tsx --env-file-if-exists=.env src/index.ts --tigris",
    "demo:sponsors": "tsx --env-file-if-exists=.env src/index.ts --glm --tigris",
    "demo:maearth": "tsx --env-file-if-exists=.env src/index.ts --maearth",
    "web": "tsx --env-file-if-exists=.env src/web/server.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "near",
    "regen",
    "agent",
    "proof-plan",
    "hackathon"
  ],
  "devDependencies": {
    "@types/express": "^4.17.25",
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1057.0",
    "apify-client": "^2.23.3",
    "express": "^4.22.2",
    "near-api-js": "^7.2.0"
  }
}
